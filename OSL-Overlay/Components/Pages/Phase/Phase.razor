@page "/phase/"
@using OSL_Overlay.GameFlow.Phase
@inject PhaseState PhaseState

@* ---------------- Phase managament ---------------- *@
<PageTitle>Phase</PageTitle>

<style>
    .section {
        scroll-margin-top: 10vh;
    }
</style>

<MudText Typo="Typo.h2" GutterBottom="true" Style="text-decoration-line: underline; font-weight:bold;"><b>Phase management</b></MudText>
<br />

<MudText Id="text" Class="section" Typo="Typo.h5" Style="text-decoration-line: underline; font-weight:bold;">Phase text</MudText>
<MudStack Row="true">
    @* ---------------- Text ---------------- *@
    <StyleFieldEditor Label=""
                      Type="text"
                      Value="@PhaseState.Info.Phase.Txt"
                      ValueChanged="v => PhaseState.UpdatePhaseText(v)"
                      BorderColor="#0B849E"
                      Placeholder="Swiss Group - Round 1" />
</MudStack>
<br />
<MudText Id="text" Class="section" Typo="Typo.h5" Style="text-decoration-line: underline; font-weight:bold;">Event text</MudText>
<MudStack Row="true">
    @* ---------------- Text ---------------- *@
    <StyleFieldEditor Label=""
                      Type="text"
                      Value="@PhaseState.Info.Event.Txt"
                      ValueChanged="v => PhaseState.UpdateEventText(v)"
                      BorderColor="#0B849E"
                      Placeholder="2025 OcciLan" />
</MudStack>
<br />
<MudText Id="text" Class="section" Typo="Typo.h5" Style="text-decoration-line: underline; font-weight:bold;">Date text</MudText>
<MudStack Row="true">
    @* ---------------- Text ---------------- *@
    <StyleFieldEditor Label=""
                      Type="text"
                      Value="@PhaseState.Info.Date.Txt"
                      ValueChanged="v => PhaseState.UpdateDateText(v)"
                      BorderColor="#0B849E"
                      Placeholder="24 Oct" />
</MudStack>

@code{
    private void OnStyleChanged(Action<string> setter, string newValue)
    {
        setter(newValue);
    }

    protected override void OnInitialized()
    {
        PhaseState.OnChange += RefreshUI;
    }

    private void RefreshUI()
    {
        InvokeAsync(StateHasChanged);
    }

    public void Dispose()
    {
        PhaseState.OnChange -= RefreshUI;
    }
}
